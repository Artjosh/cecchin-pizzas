import { redirect } from "next/navigation";
/** O checklist faz parte do mesmo evento e da mesma escala de Minha rota. */
export function StaffChecklistView(){redirect("/operacional/minha-rota?aba=checklist");}
