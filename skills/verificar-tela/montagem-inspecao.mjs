import {chromium} from './.runtime/node_modules/playwright/index.mjs';
import {cookiesDeSessao} from './sessao.mjs';
import {execFileSync} from 'node:child_process';
const db=s=>execFileSync('docker',['exec','supabase_db_Nicolas','psql','-U','postgres','-d','postgres','-Atc',s]).toString().trim();
const browser=await chromium.launch({executablePath:'C:/Users/josh/AppData/Local/ms-playwright/chromium-1243/chrome-win64/chrome.exe'});
try{const context=await browser.newContext({viewport:{width:1440,height:900}});await context.addCookies(await cookiesDeSessao('http://localhost:3000',db("select email from usuario where papel='admin' limit 1")));const page=await context.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://localhost:3000/operacional/despacho');await page.waitForTimeout(1800);await page.evaluate(()=>document.documentElement.setAttribute('data-tema','escuro'));const select=page.locator('select').first();console.log(JSON.stringify({selects:await page.locator('select').count(),qrButtons:await page.getByRole('button',{name:'Mostrar QR para impress\u00e3o',exact:true}).count(),style:await select.evaluate(el=>({color:getComputedStyle(el).color,background:getComputedStyle(el).backgroundColor,optionColor:getComputedStyle(el.options[0]).color,optionBackground:getComputedStyle(el.options[0]).backgroundColor})),errors}));await page.getByRole('button',{name:'Mostrar QR para impress\u00e3o',exact:true}).first().click();await page.getByRole('img',{name:'QR de sa\u00edda do evento',exact:true}).first().waitFor();console.log('QR na agenda renderizado');await page.screenshot({path:'skills/verificar-tela/capturas/agenda-contraste.png',mask:[page.locator('header').first(),page.locator('select'),page.locator('article')]});}finally{await browser.close();}
